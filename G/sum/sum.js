/**
 *
 *
 * @param {number} a First number
 * @param {number} b Second number
 * @returns {number} The result of usuing addition on a and b
 */
function sum(a, b) {
    return a + b;
}

module.exports = sum;
